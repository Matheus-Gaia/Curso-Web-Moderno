// CÓDIGO NÃO EXECULTÁVEL!!!

// Procedural
processamento(valor1, valor2, valor3)// => a função manipula os dados.

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    } // A função está contida nos dados(objeto).
}

obejto.processamento()// O foco passou a ser o objeto.

// Pricípios importantes:
// 1. abstração (transformar objetos reais em código)
// 2. encapsulamento (o usuário não precisa saber os níveis internos do objeto para usá-lo)
// 3. herança (prototype / tem ''1'') 
// 4. polimorfismo (conceito genérico que pode apontar para diversos objetos) => ex: carro(genérico) -> ferrari 
//                                                                                   carro(genérico) -> uno                                                                                                           
//                                                                                   carro(genérico) -> gol                                                                                                           